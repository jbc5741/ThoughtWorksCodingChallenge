import java.util.LinkedList;

/**
 * Edge.java : Represents a route taken between two nodes. Has variables for the source and destination nodes, as well
 * as a LinkedList of all nodes visited. Also contains an integer representing the total distance travelled.
 *
 * @author Jesse Cox
 * @version 1.0
 */
public class Route {
    private Node source;
    private Node destination;
    private LinkedList<Node> nodeList;
    private int length;

    /**
     *
     */
}
